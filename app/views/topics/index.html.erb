<DOCTYPE html>
<html>
  <head>
    <title>トピック一覧</title>
  </head>

  <body>
    <h1>トピック一覧</h1>
    <ul>
      <% @topics.each do |topic| %>
        <li><%= link_to topic.title, topics_show_path(topic.id) %></li>
      <% end %>
    </ul>
  </body>
<html>
